"""contrax.compat — optional interoperability bridges.

Submodules are not imported automatically. Import them explicitly when the
corresponding optional dependency is installed:

    from contrax.compat.python_control import from_python_control, to_python_control
"""
